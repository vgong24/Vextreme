Pill toggle for binary settings — the one deliberately fluid (fully rounded) control in the form set, an intentional soft note against otherwise sharp chrome.

```jsx
<Switch label="Auto-continue on verified steps" checked={on} onChange={setOn} />
```
