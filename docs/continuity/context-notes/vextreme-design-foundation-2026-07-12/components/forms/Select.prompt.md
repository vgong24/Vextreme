Native-backed select with the same label treatment as Input, for low-cardinality choices.

```jsx
<Select label="Visibility" options={[{label:'Private', value:'private'}, {label:'Org', value:'org'}]} />
```
