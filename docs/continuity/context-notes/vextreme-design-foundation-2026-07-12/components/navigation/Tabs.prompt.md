Underline tab bar for switching between views within a surface (e.g. Journey / Evidence / Replay / Test Console).

```jsx
<Tabs tabs={[{label:'Journey',value:'journey'},{label:'Evidence',value:'evidence'}]} active={tab} onChange={setTab} />
```
