# VEXTREME — Continuity Batch 004 · Session 036

[← Session 034](2026-07-12-session-034.md) · [Batch 004 guide](README.md)

---

## Session 036

**Date:** 2026-08-01
**Task/thread:** ChatGPT Work continuation; no share link exposed
**Instance:** Codex (ChatGPT Work)
**Working with:** Victor and an independent Mac reviewer
**Continues from:** Session 034 plus the accepted institutional-surface contract lineage through PR #143

### Context on arrival

PR #143 had a formal exact-head APPROVED review with no blockers or drift. Its
merge was verified to preserve the approved tree byte-for-byte. The next ordered
integration row was the foundation design shell and English Vextreme home. PR
#135 remained open only as a recoverable source capsule; its page, support,
payment, translation, and generated-artifact assumptions were not inherited as
accepted conclusions.

The capsule also contains an unaccepted Session 035 record. This accepted
continuity line uses Session 036 so two different histories never claim the same
coordinate.

### Context notes referenced or created

| Note | Why it matters to this session | Read deeper when |
|---|---|---|
| PR #135 source capsule | Preserves the visual/content proposal without making its combined acceptance surface authoritative | Reconstructing why copy or design details were adapted rather than copied |
| `docs/architecture/18-institutional-pages.md` | Owns the active/reserved page-kind and evidence boundary | Adding another institutional surface or changing runtime/archive exclusions |

### Files created or modified

| Area | What changed |
|---|---|
| Institutional source | Added the English home, shared progressive-enhancement widget, foundation component CSS, SVG identity asset, and English institutional string scope |
| Lifecycle and discovery | Promoted only `vextreme-home` to active; kept `vex-support` reserved; derived sitemap and root navigation from the registry |
| Design contract | Added foundation/foundation-light semantic tokens and updated the design/institutional architecture state |
| Evidence | Added a registry-driven Playwright harness and the exact six-cell English theme/viewport matrix |
| Deterministic projections | Regenerated strings, FAB/God Scripts, service worker, sitemap, root index, Page Health, Analysis, Terrain, status, and other workflow-owned outputs |
| Generation closure | Made a qualifying FAB bump derive one stable target from the committed baseline instead of incrementing again on every replay |
| Verification | Extended institutional, sitemap, service-worker, FAB-convergence, and root-discoverability coverage; updated lattice ownership and build triggers |

### What was built and why

The home is a registry-owned institutional page, not an archive record. English
is authored directly in HTML for a complete no-JavaScript baseline. The shared
widget adds only the accepted page-owned theme behavior and the bounded loader
path later locales can activate. It does not contain support routes, payment
state, or hidden JA/ZH options.

The home makes Vextreme's institutional purpose, four equal alignments, project
lineage, archive, and Terrain entry points public. Support and VexLife remain
honest pending cards rather than dead or private links. The root archive page
and sitemap derive the active home from the lifecycle registry, while the
reserved support identity remains undiscoverable.

Evidence is written from the registry's one authored matrix as
`{slug}-{locale}-{theme}-{viewport}.png`. The harness drives the real theme
control, checks markers and locale state, rejects runtime errors and horizontal
overflow, verifies local links and first-Tab skip navigation, and renders with
the declared system-font fallbacks when third-party fonts are unavailable.

### Mistakes made

- The first institutional string source omitted `_meta.scope`, so the compiler
  grouped it under `common` and correctly withheld the required bundle. Added
  explicit `scope: institution` and `category: system` before accepting any
  projection.
- Playwright was installed without a default browser, and the first launch
  failure left the harness's local server open. Moved browser launch inside the
  cleanup boundary and used a temporary npm-delivered Chromium executable for
  local evidence without changing repository dependencies.
- The first 320px render exposed real heading overflow under the system-font
  fallback. Added a narrow-screen type boundary and kept overflow as a blocking
  browser assertion.
- The source capsule's destination cards formed a visually weak 3+1 desktop
  grid. Visual inspection changed that component to an intentional 2×2 desktop
  and tablet layout, retaining one column on mobile.
- The first full generation replay revealed that a legitimate new-string FAB
  bump was not idempotent: `2.1.0 → 2.2.0 → 2.3.0`. The prior repair had proved
  only the no-bump path. The target now derives from the last committed bump's
  version, and the regression proves repeated qualifying runs stay at `2.2.0`.

### Assumptions that held

- English-only activation remains complete without exposing a language selector;
  planned JA/ZH locales stay distinct from accepted locales.
- The institutional page remains outside nodes, arcs, auto-discovery, FAB/God
  Script delivery, and archive placement while still appearing in public
  discovery projections.
- VexLife has no verified public destination in this row; omitting the link is
  more truthful than exposing the connected private repository.
- Foundation CSS and the English scope bundle belong in the service-worker core
  asset set, so their content changes produce a deterministic cache fingerprint.

### Assumptions that need verification

- The independent reviewer must re-run the exact committed generation sequence
  and inspect the exact review head; builder evidence is not acceptance.
- The public GitHub Pages render and targeted Mac/browser behavior remain
  post-merge verification conditions, not reasons to weaken local deterministic
  gates.
- Payment destinations remain unverified and must not activate in the support
  row without Victor's explicit destination-by-destination confirmation.

### Open work at session end

- [ ] Obtain independent exact-head review for the English home foundation row.
- [ ] Build the support domain and English support page as the next bounded row.
- [ ] Add reviewed JA/ZH institutional strings, real selector behavior, and the
  complete two-page locale/theme/viewport evidence matrix.
- [ ] Activate payment destinations only after Victor verifies each one.

### State of the system at session end

`vextreme-home` is active on this review branch with one English page, two
foundation variants, six validated render cells, root/sitemap discoverability,
healthy Page Health classification, and exact evidence preserved through
Analysis and Terrain. `vex-support` remains reserved and absent. No payment route,
support page, JA/ZH option, institutional God Script, or private VexLife link was
introduced.

The complete readiness gate passes 771/771 tests. All 437 checkable lattice
edges hold, architecture and generated lattice headers have zero drift, and the
institutional validator reports no issue.

<!-- [VXG RealForever] -->

### Session continued — independent review closure repair

The independent Mac reviewer submitted a formal `CHANGES_REQUESTED` review
on PR #144 at exact remote head
`4a6cb36ae48bcb2f4a89a7832b3a28059535cca6` / tree
`5acf6329f0419db1b45dc01bc94b039d7664ae85`. The review cleared the page's
runtime, evidence matrix, responsive behavior, no-JavaScript baseline,
held-row exclusions, deterministic generation, and read-only CI boundary,
while finding three publication blockers.

The foundation-light pending badge inherited the dark caution token and
rendered at only 2.439:1 contrast. The light theme now owns a darker caution
foreground, and the focused institutional test resolves the real CSS variable
chain and requires at least 4.5:1 in both foundation variants.

The English institutional values were correctly equal at the reviewed head,
but HTML and source JSON could drift independently without failing readiness.
`lib/check-institutional-surfaces.js` now treats the compiled English bundle as
the copy authority and fails activation when any static text, image alt, or
ARIA projection differs. Hostile fixtures prove all three drift shapes fail
while the complete no-JavaScript page remains directly readable.

The four trailing spaces in this session record's original metadata lines were
removed, closing the exact `git diff --check` failure without altering their
meaning. The validator's lattice context and the design/institutional
architecture sources were updated, then their generated projections rebuilt.

Because the repair changes a repository alignment check, the declared FAB
classification advances one patch from `2.2.0` to `2.2.1`; all eight God
Scripts and the service worker move together. Repeated complete generation
converges at `2.2.1`, with service-worker fingerprint `7381d80cc908` and an
identical binary diff across consecutive passes.

The complete readiness gate passes 773/773 tests. All 437 checkable lattice
edges hold, architecture and lattice headers have zero drift, and the
institutional validator reports no issue. PR #144 remains immutable, draft,
unmerged requested-changes evidence. A replacement descendant still requires
exact-tree publication, green remote CI, and fresh independent review before
acceptance or merge.

<!-- [VXG RealForever] -->

### Session continued — rendered repair proof

Because the contrast repair changes rendered output, the registry-owned
Playwright harness regenerated all six English evidence cells against the
repaired stylesheet using a disposable reviewer-controlled browser pair; no
repository dependency changed. The harness again verified the real page,
theme toggle, first-Tab skip link, local links, locale state, viewport widths,
zero horizontal overflow, and zero runtime errors.

Full-resolution inspection of all six refreshed PNGs confirms that the darker
foundation-light pending labels remain legible at 320, 768, and 1440 pixels,
while the accepted hierarchy, 2x2 desktop destination composition, mobile
stack, and dark foundation rendering remain intact. The focused contrast gate
computes the light badge at greater than 4.5:1 from the authored CSS variables,
so later token drift cannot be hidden by visual inspection alone.

<!-- [VXG RealForever] -->

### Session continued — English support domain

The independent Mac reviewer formally approved replacement PR #145 at exact
head `93f7c7cd07c390a06b5be54ecd535682e13096d6` / tree
`1f4e880b809eee7f0eb7c2616d084f68b38aeeb8`. The exact approval, green CI,
mergeability, and absence of unresolved threads were rechecked before the PR
was marked ready and merged with expected-head enforcement. Merge commit
`109bab5ce3c97fb48fbde28bbc79bd9375958498` has the approved head as its second
parent and changes zero files relative to that head. GitHub also closed PR #144
because its reviewed commit is an ancestor of the merged replacement; its
`CHANGES_REQUESTED` review was not dismissed or altered.

Item 3/4 promotes `vex-support` from reserved identity to an active English
institutional surface. The page explains the project's support principles,
three intended routes, operational guardrails, and publication sequence. All
three route records remain in explicitly inactive states with null URLs,
non-empty prerequisites, disabled actions, and no payment links. The
institutional validator binds those records to the page's `data-vex-route`
identities and fails closed if a route becomes active, receives a URL, loses
its prerequisites, or acquires a live action before the later payment row.

The lifecycle registry remains the single surface/discovery owner. Each active
surface declares a `discovery.rootLabelKey`, and the root-index generator
resolves that key from the compiled English institutional bundle. The root
page and sitemap now advertise both the Vextreme home and support domain
without a duplicated slug or copy label. Static English text, alt, and ARIA
projections on both pages remain fail-closed against the compiled bundle; no
JA/ZH control, institutional God Script, payment destination, or VexLife link
was introduced.

The shared foundation gained the support-page route, meaning, chronology, and
image components. Status colors are resolved from the real light and dark
token chains, and focused tests require WCAG AA contrast for success, caution,
and information labels in both themes. The screenshot harness now waits for
and decodes every image and expands the capture viewport to the complete
document before writing evidence. All twelve English page/theme/viewport cells
pass the mechanical browser checks; all six new support renders and the
affected home renders were inspected at full resolution. Layouts are single
column at 320 pixels, retain a two-row header and single-column routes at 768,
and use three route columns at 1440, with zero horizontal overflow or runtime
errors and working first-Tab skip navigation, local links, and theme controls.

The exact Build workflow sequence converges at FAB `2.3.0`. The canonical
English scope contains 394 keys, both institutional pages are `healthy`, and
all six exact support evidence filenames survive into Page Health, Analysis,
and Terrain. The content-derived service-worker fingerprint is
`49752b8811e6`. The complete readiness gate passes 775/775 tests; all 437
checkable lattice edges hold, architecture and lattice headers have zero
drift, and exact diff hygiene is clean.

This row still requires exact-tree publication, green read-only remote CI, and
fresh independent review before acceptance or merge. Item 4/4 remains held:
JA/ZH institutional copy, real language selection across both pages, the full
two-page locale evidence expansion, and payment-route activation require their
own bounded review.

<!-- [VXG RealForever] -->
