# VEXTREME — Continuity Batch 004 · Session 040

[← Session 039](2026-08-06-session-039.md) · [Batch 004 guide](README.md)

---

## Session 040

**Date:** 2026-08-07
**Task/thread:** Terrain Relational Projection Row B — adaptive stage focus composition
**Instance:** ChatGPT/GitHub collaborator (`instance.coder.terrain-row-b.75fd1c92-3940-44e7-8fef-fff5b49574a4`)
**Working with:** Victor as authenticated Mac host operator; Operations owns return consumption, hosted observation, fresh Assurance, and lifecycle routing
**Continues from:** merged Row A PR #150 / public main `6940e442fef20b618c43ec8bbc19a5b40bfef309`, fresh Row B admission comment 5223645444, and draft Row B PR #153

### Context on arrival

Row A had already made the system → stage → node threshold visible and proven exact 390px viewport truth. Fresh Row B admission then re-ran authenticated `npm run current-work` and captured current Code/Content Stage views on exact public main. The baseline was technically contained but semantically tiny: Stage overview still paid full-list geometry, collapsing narrow labels to a few pixels.

### Candidate direction

This candidate keeps the accepted Terrain data/generator and Journey v1 source unchanged. The read side now treats Stage as two semantic substates inside the existing `level=stage` coordinate:

```text
SYSTEM
  -> bounded Stage overview cards
  -> focused Stage + compact sibling context rails
  -> NODE with selected-node neighborhood priority + viewport traversal
```

The focused Stage is carried by the already-existing `stageKey` semantic field. Zooming into Stage first selects/focuses a stage without inventing a fourth Journey level; another semantic zoom enters Node. Zoom-out reverses the same path. Browser Back/Forward therefore restores overview versus focused Stage through existing version-1 semantic state rather than a schema migration.

### Source boundary

Mutated source is limited to `pages/terrain-map.html`, `tests/45-terrain-journey.test.js`, this session file, `docs/continuity/INDEX.md`, and the exact eight Row B screenshot paths claimed by PR #153. `lib/terrain-journey.js`, `lib/build-terrain-map.js`, `tests/28-build-terrain-map.test.js`, `data/terrain-map.json`, canonical Terrain data, lattice source, localization, shared tokens, shell/FAB, workflows, SDK, VexLife, and private data remain unchanged.

### Verification contract

The Mac Coder relay must pass focused Journey/Terrain tests and full `npm run pr-ready`, capture the eight exact claimed screenshots, and return machine-readable same-carrier receipts for CSS/visual viewport dimensions, Stage/Node DOM counts, focus semantics, keyboard/touch target behavior, accessibility names, reduced motion, Code/Content parity, and a synthetic larger-scale envelope. Remote hosted workflows remain Operations-owned after the ordinary non-force push.

### Held effects

Approval, ready transition, merge, post-merge publication effects, force push, rebase, reset, amend, squash, and history rewrite remain held. A green candidate still requires fresh claimless exact-head Independent Assurance before any later lifecycle decision.


### Fresh Assurance correction

Fresh claimless Independent Assurance on published head 5f9e7f9462881701aecc59c4dfd11a1b87d651a6 returned CHANGES_REQUIRED in review 4887723392. Three bounded findings are corrected on the same Row B lane: zero-member Content stages remain visible as truthful zero-count semantic cards instead of invisible selectable geometry; selected-node entry re-frames and re-materializes around a bounded neighborhood while preserving viewport traversal for farther material; and every browser evidence cell now binds and asserts the final light/dark theme state in the same carrier as its PNG. No generator, canonical Terrain data, Journey version, localization, shared shell/FAB, workflow, SDK, or VexLife scope is added.

<!-- [VXG RealForever] -->
